<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Funding</title>
    <link rel="stylesheet" href="/css/create_real_funding.css">
</head>
<body>
    <div class="container">
        <h1><%= product.seller %>의 <%= product.productName %>을 펀딩하고 있어요!</h1>
        <form action="/create_funding" method="POST">
            <div class="form-group">
                <label for="fundingDate">펀딩 일자:</label>
                <input type="text" id="fundingDate" name="fundingDate" value="<%= new Date().toISOString().split('T')[0] %>" readonly>
            </div>
            <div class="form-group">
                <label for="people">참여 인원:</label>
                <input type="number" id="people" name="people" min="1" required>
            </div>
            <div class="form-group">
                <label for="deliveryDate">상품 배송 예정 일자:</label>
                <input type="date" id="deliveryDate" name="deliveryDate" required>
            </div>
            <div class="form-group">
                <label for="unitPrice">가격:</label>
                <input type="text" id="unitPrice" name="unitPrice" value="<%= product.unitPrice %> 원 / <%= product.unit %>" readonly>
            </div>
            <div class="form-group">
                <label for="representative">대표자:</label>
                <input type="text" id="representative" name="representative" value="<%= representative.name %>" readonly>
            </div>
            <div class="form-group">
                <label for="representativePhone">대표자 전화번호:</label>
                <input type="text" id="representativePhone" name="representativePhone" value="<%= representative.phone %>" readonly>
            </div>
            <div class="form-group">
                <label for="representativeAddress">대표자 주소:</label>
                <input type="text" id="representativeAddress" name="representativeAddress" value="<%= representative.address %>" readonly>
            </div>
            <div class="form-group">
                <label for="distributionLocation">배부 위치:</label>
                <input type="text" id="distributionLocation" name="distributionLocation" required>
            </div>
            <div class="form-group">
                <label for="distributionDate">배부 일자 및 시간:</label>
                <input type="datetime-local" id="distributionDate" name="distributionDate" required>
            </div>
            <input type="hidden" name="productId" value="<%= product.fundingProductId %>">
            <button type="submit">펀딩 생성</button>
        </form>
    </div>
</body>
</html>
